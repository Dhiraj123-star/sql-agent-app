services:
  sql-agent:
    build: .
    container_name: sql-agent-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/data
    restart: always

volumes:
  sqlite_data: 