services:
  sql-agent:
    build: .
    container_name: sql-agent-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - sqlite_data:/app
    restart: always

volumes:
  sqlite_data: 